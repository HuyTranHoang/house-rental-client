import { Form, FormInstance } from 'antd'

function PostPropertyOverview({ form }: { form: FormInstance }) {
  return <Form form={form}>Lấy thông tin từ form hiển thị lên</Form>
}

export default PostPropertyOverview
